import { Logo } from "../Navbar/styledNavbar";
import {
    FooterContainer,
} from "./styledFooter";

export const Footer = () => {
    return (
        <FooterContainer>
            <Logo>land page</Logo>
        </FooterContainer>
    );
};